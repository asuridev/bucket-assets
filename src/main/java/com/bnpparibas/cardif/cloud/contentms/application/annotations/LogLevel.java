package com.bnpparibas.cardif.cloud.contentms.application.annotations;

/** Nivel con el que {@link LogExceptions} registra la excepcion. */
public enum LogLevel {
    DEBUG,
    INFO,
    WARN,
    ERROR
}
