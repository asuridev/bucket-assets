{
  "name": "Secreto kv contentms-secrets",
  "metadata": {
    "comment": "PLANTILLA: up.sh la renderiza en mappings/secret-kv.json sustituyendo los marcadores de usuario y contrasena de MinIO, que salen de las mismas variables que services/minio.yaml para que no puedan derivar. El fichero renderizado SE CONSERVA entre ejecuciones de up.sh: es el sitio donde se editan los valores a mano, y ./reload-secret.sh hace que el stub los relea sin recrear el contenedor. Para volver a esta plantilla: ./up.sh --regen-secret ibm-secret-manager. No la pongas dentro de mappings/: WireMock aborta el arranque si encuentra ahi un fichero que no sepa parsear. La ruta de urlPath tiene que coincidir con SECRETS_GROUP/SECRETS_NAME: WireMock casa por ruta literal."
  },
  "request": {
    "method": "GET",
    "urlPath": "/api/v2/secret_groups/default/secret_types/kv/secrets/contentms-secrets"
  },
  "response": {
    "status": 200,
    "headers": {
      "Content-Type": "application/json"
    },
    "jsonBody": {
      "id": "00000000-0000-0000-0000-000000000001",
      "name": "contentms-secrets",
      "secret_type": "kv",
      "secret_group_id": "default",
      "description": "Secretos de ContentMS (stub local)",
      "state": 1,
      "state_description": "active",
      "versions_total": 1,
      "created_by": "wiremock",
      "created_at": "2026-01-01T00:00:00Z",
      "updated_at": "2026-01-01T00:00:00Z",
      "crn": "crn:v1:bluemix:public:secrets-manager:us-south:a/stub:stub:secret:00000000-0000-0000-0000-000000000001",
      "data": {
        "MINIO_ACCESS_KEY": "__MINIO_USER__",
        "MINIO_SECRET_KEY": "__MINIO_PASSWORD__",
        "COS_API_KEY": "stub-cos-api-key",
        "COS_SERVICE_INSTANCE_ID": "crn:v1:bluemix:public:cloud-object-storage:global:a/stub:stub::",
        "REDIS_PASSWORD": ""
      }
    }
  }
}
