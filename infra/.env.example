# Datos del workspace de DevX. up.sh los pide la primera vez y guarda este fichero como
# infra/.env (ignorado por git), asi que normalmente no hay que crearlo a mano.
#
# Se usan para construir las URLs publicas: https://$DEVX_HOST/user/$DEVX_USER/http/<puerto>/
DEVX_USER=j31399
DEVX_HOST=devx-cardif04.staging.echonet
